<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :add_line %><br />
    <%= f.text_field :add_line %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :state_add %><br />
    <%= f.text_field :state_add %>
  </div>
  <div class="field">
    <%= f.label :zip_add %><br />
    <%= f.number_field :zip_add %>
  </div>
  
  <%= f.fields_for :phone_lists do |pf|%>
    <div class="field">
    <%= pf.label :area_code %><br />
    <%= pf.number_field :area_code %>
  </div>
  <div class="field">
    <%= pf.label :phone_num %><br />
    <%= pf.number_field :phone_num %>
  </div>
<%end%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
